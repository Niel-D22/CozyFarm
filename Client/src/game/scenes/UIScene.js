import Phaser from 'phaser';

export default class UIScene extends Phaser.Scene {
  constructor() {
    super('UIScene');
  }

  create() {
    // UIScene created successfully. Available for secondary HUD overlays.
  }
}
